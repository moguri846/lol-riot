import styled from "styled-components";

const InputFormContainer = styled.div`
  width: 100%;
  display: flex;

  & > input {
    flex: 8;
  }

  & > button {
    flex: 1.2;
  }
`;

export { InputFormContainer };
